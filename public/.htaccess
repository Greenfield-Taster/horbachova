# Простой .htaccess для диагностики
RewriteEngine On

# Главная страница
DirectoryIndex index.html

# SPA routing
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
